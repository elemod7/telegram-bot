#!/bin/bash

# Укажи имя пользователя, под которым работает бот
USER="myuser"

echo "🔧 Устанавливаем владельца и права для конфигурационных файлов..."

# Найти и обновить владельца для всех нужных файлов
find . -type f \( -name "*.json" -o -name "*.db" -o -name "*.log" \) -exec sudo chown $USER:$USER {} \;

# Обновить права на чтение/запись
find . -type f \( -name "*.json" -o -name "*.db" -o -name "*.log" \) -exec chmod 664 {} \;

echo "✅ Готово! Права и владельцы обновлены."
